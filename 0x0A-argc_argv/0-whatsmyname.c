#include "main.h"
#include<stdio.h>

/**
 * main - prints its name
 * @argc: number of arguments
 * @argv: array
 * Return: return 0
*/
int main (int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
